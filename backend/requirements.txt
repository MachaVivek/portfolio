# Python packages this app needs. Installed into the Docker image at build time.
#
# Note: the langchain/supabase packages use ">=" rather than exact pins, so a
# rebuild can pick up newer versions. If a future build ever breaks unexpectedly,
# pin the versions that currently work (pip freeze shows them).

# --- Web server ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic-settings==2.5.2

# --- AI and RAG ---
langchain-core>=0.3
langchain-google-genai>=2.0        # Gemini chat + embeddings
langchain-qdrant>=0.2              # connects LangChain to Qdrant
langchain-text-splitters>=0.3      # cuts documents into chunks
qdrant-client>=1.12                # vector database

# --- External services ---
httpx>=0.27                        # HTTP calls to the GitHub API
resend>=2.0                        # sending email
supabase>=2.0                      # Postgres database

# --- Protection ---
slowapi>=0.1.9                     # per-IP rate limiting
